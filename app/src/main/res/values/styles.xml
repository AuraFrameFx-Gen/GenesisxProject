<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Utility styles for overlays, dialogs, agent popups -->
    <style name="AgentOverlay">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="AgentDialog">
        <item name="android:windowMinWidthMajor">90%</item>
        <item name="android:windowMinWidthMinor">90%</item>
        <item name="android:background">@color/terminal_background</item>
        <item name="android:windowBackground">@color/terminal_background</item>
        <item name="android:padding">24dp</item>
        <item name="android:elevation">8dp</item>
        <item name="android:textColor">@color/terminal_green</item>
    </style>
</resources>